package main

// version is the released version of the binary. release-please rewrites the
// literal below when it prepares a release, so every build -- local, CI and
// container -- reports the tag it was cut from. See "extra-files" in
// release-please-config.json.
const version = "1.2.0" // x-release-please-version
